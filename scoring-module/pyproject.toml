[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "scoring-module"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40.0",
    "httpx>=0.27.0",
    "pyyaml>=6.0",
    "sqlalchemy>=2.0",
    "typer>=0.12",
    "harness-db",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "nox>=2024.3.2", "ruff>=0.6"]

[tool.hatch.build.targets.wheel]
packages = ["scoring_module"]

[tool.ruff]
line-length = 100
lint.extend-select = ["I"]
