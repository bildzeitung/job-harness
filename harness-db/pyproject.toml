[project]
name = "harness-db"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "sqlalchemy>=2.0",
]

[tool.hatch.build.targets.wheel]
packages = ["harness_db"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
